// operadores aritmeticos//

// + soma
// - subtração
// * multiplicação
// / divisão
// % resto da divisão - modulo
// ** potenciação


let sum = 10 + 5;
let sub = 10 - 5;
let mult = 10 * 5;
let div = 10 / 5;
let mod = 10 % 3;
let exp = 2 ** 3;

// sum.push(+ 5); // isso não é possível, pois sum é um número, e números não tem o método push, apenas listas tem esse método, por isso da erro //

console.log(`Soma: ${sum}`);
console.log(`Subtração: ${sub}`);
console.log(`Multiplicação: ${mult}`);
console.log(`Divisão: ${div}`);
console.log(`Resto da divisão: ${mod}`);
console.log(`Potenciação: ${exp}`); 

